<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Campground</title>
</head>
<body>
    <form action="/campgrounds/editCamp/<%= camp._id %>?_method=PUT" method='POST'>
        <div>
            <label for="title">Title</label>
            <input type="text" id='title' name='title' value='<%= camp.title %>'>
        </div>
        <div>
            <label for="location">Location</label>
            <input type="text" id='location' name='location' value='<%= camp.location %>'>
        </div>
        <button type="submit">Update</button>
    </form>
    <a href="/campgrounds/oneCamp/<%= camp._id %>">Back To Camp</a>
</body>
</html>